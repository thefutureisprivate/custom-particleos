# Remote SSH is closed by default. Copying this file to /etc is handled by
# systemd-tmpfiles. Add individual addresses or narrow CIDRs to the mutable copy.
set ssh_ipv4 {
    type ipv4_addr
    flags interval
}

set ssh_ipv6 {
    type ipv6_addr
    flags interval
}
