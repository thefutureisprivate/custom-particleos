# SPDX-License-Identifier: LGPL-2.1-or-later

[Home]
# Require password-encrypted per-user images even though the dedicated backing
# partition is itself TPM2 encrypted.
DefaultStorage=luks
DefaultFileSystemType=btrfs
