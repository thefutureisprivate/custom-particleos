# SPDX-License-Identifier: LGPL-2.1-or-later

[Service]
# Locale, keymap and timezone are unnecessary VPS installer questions. Retain
# the upstream first-boot root-password workflow without embedding a secret.
ExecStart=
ExecStart=/usr/bin/systemd-firstboot --prompt-root-password --mute-console=yes
