# SPDX-License-Identifier: LGPL-2.1-or-later

# systemd-pcrextend enumerates NV-index authorization material here when it
# measures signed service images. Create the directory before RootImage setup
# rather than granting the helper general create access below /run/systemd.
d /run/systemd/nvpcr 0755 root root -
