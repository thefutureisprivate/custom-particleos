# SPDX-License-Identifier: LGPL-2.1-or-later
#
# The initrd creates these trees before the real SELinux policy is loaded.
# Recursively restore their policy labels before the host udev daemon starts.
Z /dev - - - -
Z /run/udev - - - -
