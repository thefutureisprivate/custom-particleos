# SPDX-License-Identifier: LGPL-2.1-or-later

# The installer boots with root=tmpfs. Create the conventional ESP mount point
# before the installer media service establishes its filesystem sandbox.
d /efi 0755 root root -
