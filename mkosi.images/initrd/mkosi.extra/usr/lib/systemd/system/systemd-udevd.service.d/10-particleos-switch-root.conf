# SPDX-License-Identifier: LGPL-2.1-or-later

[Service]
# Preserve queued-event descriptors across crashes, but clear them when udev is
# deliberately stopped for switch-root so no pre-policy AF_NETLINK fd survives.
FileDescriptorStorePreserve=restart
