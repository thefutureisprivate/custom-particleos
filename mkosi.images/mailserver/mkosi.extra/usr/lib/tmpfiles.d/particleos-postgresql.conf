# SPDX-License-Identifier: LGPL-2.1-or-later

# Keep the mount target available without ever treating the root filesystem as
# a valid archive destination; enable-pitr additionally requires a mount point.
d /var/lib/pgsql/backup 0700 postgres postgres -
