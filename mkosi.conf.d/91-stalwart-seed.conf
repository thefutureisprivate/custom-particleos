# SPDX-License-Identifier: LGPL-2.1-or-later

# The recovery-seed package has a distinct signed metadata contract from the
# moving application-image channel and therefore selects its own subimage.
[Match]
PathExists=/usr/src/packages/SOURCES/stalwart-seed.build

[Config]
Dependencies=
Dependencies=stalwart-seed
